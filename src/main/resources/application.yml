server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/BrayanHF/distributor-config-data
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}